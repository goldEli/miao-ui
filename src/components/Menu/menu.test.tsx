import { faItalic } from "@fortawesome/free-solid-svg-icons"
import React from "react"
import Menu from "./menu"

describe("test Menu and MenuItem componnent in default(horizontal) mode", () => {
  it('should render correct Menu and MenuItem baseed on default props', () => {

  })
  it("click items should change active and call the right callback", () => {

  })
  it('should show dropDown itmes when hover on subMenu', () => {

  })
})

describe('test Menu and MenuItem component in vertical mode', () => {
  it("should render vertical mode when mode is set to vertical", () => {

  })
  it("should show dropdown items click on subMenue for vertical mode", () => {

  })
  it("should show subMenu dropdown when defaultOpenSubMenus contains SubMenu index", () => {

  })
})